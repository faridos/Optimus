 <form action="{{ path("add_reward_club", {'id' : club.id }) }}" {{ form_enctype(form) }} method="post" class="form-horizontal0 form-groups-bordered">
                                    <br>
                                    {{ form_errors(form) }}
                    <div class="well">
                     


                        <div class="form-group">

                            <div class="col-md-12 form-group">
                                <div class="col-md-3">
                                    <label class="control-label">Titre :</label>
                                </div>
                                <div class="col-md-9">
                                    {{ form_widget(form.titre) }}
                                </div>
                            </div>

                            <div class="col-md-12 form-group">
                                <div class="col-md-3">
                                    <label class="control-label">Classement:</label>
                                </div>
                                <div class="col-md-9">
                                    {{ form_widget(form.classment) }}
                                </div>
                            </div>
                            <div class="col-md-12 form-group">
                                <div class="col-md-3">
                                    <label class="control-label">Date :</label>
                                </div>
                                <div class="col-md-9">
                                    {{ form_widget(form.date) }}
                                </div>
                            </div>




                        </div>

                    <button type="submit" class="btn btn-blue">Enregistrer</button>
                                  {{ form_widget(form._token) }}
                    </div>
                
                    </form>
